# MongoDB
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_INITDB_ROOT_USERNAME=tree
MONGO_INITDB_ROOT_PASSWORD=tree
MONGO_INITDB_DATABASE=tree

# MongoDB Search (mongot)
MONGOT_PORT=27028

# Prefect
PREFECT_PORT=4200
PREFECT_API_URL=http://127.0.0.1:4200/api

# LLM
GOOGLE_API_KEY=your-google-api-key

# Voyage AI (for multimodal embeddings)
VOYAGE_API_KEY=your-voyage-api-key

# Modal (for Modal-hosted vLLM embedding server)
MODAL_EMBEDDING_API_KEY=your-modal-embedding-api-key

# Bright Data Web Unlocker (fallback web scraping)
BRIGHTDATA_API_KEY=your-brightdata-api-key
BRIGHTDATA_UNLOCKER_ZONE=your-brightdata-unlocker-zone

# Bright Data SERP API (on-demand web search)
BRIGHTDATA_SERP_ZONE=your-brightdata-serp-zone

# Memory-app config (optional; defaults to apps/memory/configs/default.yaml).
# Override with an absolute path, or a path relative to the cwd you run commands from.
# APP_CONFIG_PATH=apps/memory/configs/default.yaml
