# MongoDB
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_INITDB_ROOT_USERNAME=twin
MONGO_INITDB_ROOT_PASSWORD=twin
MONGO_INITDB_DATABASE=twin

# MongoDB Search (mongot)
MONGOT_PORT=27028

# Prefect
PREFECT_PORT=4200
PREFECT_API_URL=http://127.0.0.1:4200/api

# LLM
GOOGLE_API_KEY=your-google-api-key

# vLLM / Modal embedding server
VLLM_API_KEY=EMPTY

# App config (optional, defaults to configs/default.yaml)
# APP_CONFIG_PATH=configs/default.yaml
